@model IEnumerable<Clinika.Models.DatabaseObject.DoctorEntry>

@{
    ViewBag.Title = "View Doctor";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*@if (Session["LoggedCenterID"] != null)
    {

        <text> Welcome @Session["LoggedName"].ToString() </text>
        <hr />*@
<p>
    <hr />
    || @Html.ActionLink("View Doctor", "Index", "DoctorEntry", new { @class = "btn btn-primary", @style = "color:white" }) ||
    @Html.ActionLink("Add Doctor", "Create", "DoctorEntry") ||
    @Html.ActionLink("Treatment", "Index", "Treatment") ||
    @Html.ActionLink("Medicine Stock", "Index", "MdicineStockReport") ||
    <hr />
</p>

<h2>View Doctor</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Degree)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Specialization)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Degree)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specialization)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DoctorId }) |
                @Html.ActionLink("Details", "Details", new { id = item.DoctorId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DoctorId })
            </td>
        </tr>
    }

</table>
